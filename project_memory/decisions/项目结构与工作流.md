# 项目结构与工作流

项目建议按以下层次理解：

1. `data_engine/` 负责原始数据获取。
2. `market/indicators/` 负责可复用指标计算。
3. `market/services/` 负责为推送与展示整理快照数据。
4. `market/jobs/` 负责可直接运行的任务入口。
5. `backend/` 负责 HTTP 接口。
6. `frontend/` 负责展示与交互。

这样可以让任务、接口与页面的职责保持清晰。
